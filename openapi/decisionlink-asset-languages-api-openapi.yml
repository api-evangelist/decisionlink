openapi: 3.2.0
info:
  title: Value Proposition Asset Languages API
  version: 0.1.0
tags:
- name: asset_languages
paths:
  /v1/value-proposition/{valuePropId}/asset-languages:
    get:
      tags:
      - asset_languages
      summary: Get the Value proposition asset language
      description: Endpoint for reading the Value Propositions asset languages and mark the selected asset language
      operationId: get_asset_languages_v1_value_proposition__valuePropId__asset_languages_get
      security:
      - HTTPBearer: []
      parameters:
      - name: valuePropId
        in: path
        required: true
        schema:
          type: integer
          title: Valuepropid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
  securitySchemes:
    HTTPBearer:
      type: http
      description: Access token in the form of a JWT
      scheme: bearer
