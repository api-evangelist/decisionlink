openapi: 3.2.0
info:
  title: Decisionlink Expense Types API
  version: 0.1.0
  description: 'Operations tagged expense_types across 2 of this provider''s published API definitions: decisionlink-value-facts-openapi.json, decisionlink-value-proposition-openapi.json. Each path carries the servers of the definition it was published in.'
tags:
- name: Expense Types
paths:
  /v1/value-facts/expense-types:
    get:
      tags:
      - Expense Types
      summary: Get all expense types
      description: Get all expense types.
      operationId: get_expense_types_v1_value_facts_expense_types_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExpenseType'
                type: array
                title: Response Get Expense Types V1 Value Facts Expense Types Get
      security:
      - HTTPBearer: []
  /v1/value-proposition/expense-types:
    get:
      tags:
      - Expense Types
      summary: Get all expense types
      description: 'Get all expense types.


        Returns

        -------

        - list[ExpenseType]: List of all expense types.'
      operationId: get_expense_types_v1_value_proposition_expense_types_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExpenseType_2'
                type: array
                title: Response Get Expense Types V1 Value Proposition Expense Types Get
      security:
      - HTTPBearer: []
components:
  schemas:
    ExpenseType:
      properties:
        id:
          type: integer
          title: Id
          description: Unique identifier for the expense type.
        name:
          type: string
          title: Name
          description: Name of the expense type.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Description of the expense type.
        abbr:
          anyOf:
          - type: string
          - type: 'null'
          title: Abbr
          description: Abbreviation for the expense type.
      type: object
      required:
      - id
      - name
      title: ExpenseType
      description: Expense type response model.
    ExpenseType_2:
      properties:
        id:
          type: integer
          title: Id
          description: Unique identifier for the expense type.
        name:
          type: string
          title: Name
          description: Name of the expense type.
        abbr:
          anyOf:
          - type: string
          - type: 'null'
          title: Abbr
          description: Abbreviation of the expense type.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Description of the expense type.
        created:
          anyOf:
          - type: string
          - type: 'null'
          title: Created
          description: Timestamp when the expense type was created.
        modified:
          anyOf:
          - type: string
          - type: 'null'
          title: Modified
          description: Timestamp when the expense type was last updated.
      type: object
      required:
      - id
      - name
      title: ExpenseType
      description: Expense Type model
  securitySchemes:
    HTTPBearer:
      type: http
      description: Access token in the form of a JWT
      scheme: bearer
x-refined-from:
- decisionlink-value-facts-openapi.json
- decisionlink-value-proposition-openapi.json
