openapi: 3.2.0
info:
  title: Decisionlink Health API
  version: 0.1.0
  description: 'Operations tagged health across 3 of this provider''s published API definitions: decisionlink-collaboration-openapi.json, decisionlink-value-facts-openapi.json, decisionlink-value-proposition-openapi.json. Each path carries the servers of the definition it was published in.'
tags:
- name: Health
paths:
  /healthz:
    get:
      tags:
      - Health
      summary: Health
      description: Provides information about the health of the application
      operationId: health_healthz_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthCheckResponse'
components:
  schemas:
    HealthCheckResponse:
      properties:
        status:
          type: string
          title: Status
          default: OK
      type: object
      title: HealthCheckResponse
      description: Health check response
  securitySchemes:
    HTTPBearer:
      type: http
      description: Access token in the form of a JWT
      scheme: bearer
x-refined-from:
- decisionlink-collaboration-openapi.json
- decisionlink-value-facts-openapi.json
- decisionlink-value-proposition-openapi.json
